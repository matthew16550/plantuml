name: CI

on:
  pull_request:
    types: [ opened, synchronize, reopened ]
  push:
    branches: [ snapshot-release-2 ]
    tags: [ v* ]
  workflow_dispatch:

defaults:
  run:
    shell: bash

jobs:
  # We run the tests on many OS / Java combinations but also the Compile step because some users build
  # their own jars from source, so it is good for CI to check that is working on all combinations.
  build:
    if: "!( github.event.name == 'push' && startsWith(github.event.commits[0].message, '[maven-release-plugin] prepare release ') )"
    strategy:
      fail-fast: false
      matrix:
        #        java_version: [ 8, 11, 17 ]
        #        os: [ macos-10.15,  macos-11, ubuntu-18.04, ubuntu-20.04, windows-2016, windows-2019, windows-2022 ]
        include:
          - release_from_this_build: true
            os: ubuntu-20.04
            java_version: 8
    runs-on: ${{ matrix.os }}
    outputs:
      release_version: ${{ steps.configure_release.outputs.release_version }}
    steps:
      - name: Checkout the repository
        uses: actions/checkout@v2

      - name: Set up java
        uses: actions/setup-java@v2.3.1
        with:
          java-version: ${{ matrix.java_version }}
          distribution: temurin
          cache: maven

      # Downloading all the dependencies is very log spammy, so we do this in its own step.
      - name: Prime maven cache
        run: mvn --batch-mode dependency:go-offline

      # Compile / Test / Package are separate steps so the reason for any failure is more obvious in GitHub UI
      - name: Compile
        run: mvn --batch-mode compile

      - name: Test
        run: mvn --batch-mode test

      - name: Package
        if: matrix.release_from_this_build
        run: mvn --batch-mode -DfinalName=plantuml -Dmaven.test.skip=true package

      - name: Upload jar artifacts
        if: matrix.release_from_this_build
        uses: actions/upload-artifact@v2
        with:
          # Using github.run_number here to reduce confusion when downloading & comparing artifacts from several builds  
          name: ${{ github.run_number }}-jars
          path: target/*.jar

      - name: Upload jar artifact (only simple jar - without javadoc or sources)
        if: ${{ matrix.release_from_this_build }}
        uses: actions/upload-artifact@v2
        with:
          name: plantuml-jar
          path: |
            target/*.jar
            !target/*-javadoc.jar
            !target/*-sources.jar

      - name: Configure release job
        id: configure_release
        if: matrix.release_from_this_build
        run: |
          POM_VERSION="$(mvn -q -Dexec.executable=echo -Dexec.args='${project.version}' exec:exec)"

          cat <<-EOF
            GITHUB_EVENT_NAME       : '${GITHUB_EVENT_NAME}'
            GITHUB_REF              : '${GITHUB_REF}'
            GITHUB_ACTOR            : '${GITHUB_ACTOR}'
            GITHUB_REPOSITORY_OWNER : '${GITHUB_REPOSITORY_OWNER}'
            POM_VERSION             : '${POM_VERSION}'
          EOF

          # (We match against github.repository_owner as a kludge so that forked repos can release themselves when testing the workflow)

          if [[ "${GITHUB_EVENT_NAME}" == "push" && "${GITHUB_REF}" == "refs/tags/v${POM_VERSION}" && \
                && ( "${GITHUB_ACTOR}" == "arnaudroques" || "${ACTOR}" == "${GITHUB_REPOSITORY_OWNER}" ) \
             ]]; then
            echo "::notice title=::This run will release '${POM_VERSION}'"
            echo "::set-output name=release_version::${POM_VERSION}"
          else
            echo "This run will NOT make a release"
          fi

  release:
    needs: [ build ]
    if: needs.build.outputs.release_version
    runs-on: ubuntu-latest
    steps:
      - name: Checkout the repository
        uses: actions/checkout@v2

      - name: Download jars
        uses: actions/download-artifact@v2
        with:
          name: ${{ github.run_number }}-jars

      - name: Create release in GitHub
        env:
          GITHUB_TOKEN: ${{ github.token }}
          RELEASE_VERSION: ${{ needs.build.outputs.release_version }}
          RELEASE_SHA: ${{ github.sha }}
          TAG: v${{ needs.build.outputs.release_version }}
        run: |
          set -x

          mv plantuml.jar         "plantuml-${RELEASE_VERSION}.jar"
          mv plantuml-javadoc.jar "plantuml-${RELEASE_VERSION}-javadoc.jar"
          mv plantuml-sources.jar "plantuml-${RELEASE_VERSION}-sources.jar"

          gh release create --target "${RELEASE_SHA}" --title "${TAG}" "${TAG}" \
            "plantuml-${RELEASE_VERSION}.jar" \
            "plantuml-${RELEASE_VERSION}-javadoc.jar" \
            "plantuml-${RELEASE_VERSION}-sources.jar"

          echo "::notice title=::Released at ${GITHUB_SERVER_URL}/${GITHUB_REPOSITORY}/releases/tag/${TAG} 🎉"
